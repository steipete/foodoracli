version: 2

run:
  timeout: 3m
  tests: true

linters:
  disable-all: true
  disable:
    - errcheck
  enable:
    - govet
    - ineffassign
    - staticcheck
    - unused

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
